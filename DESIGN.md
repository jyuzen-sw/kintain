# 勤怠管理PoC デザイン方針

- 状態: 確定
- コンセプト: **現場の余白 / Field Calm**
- 適用範囲: 従業員向けPWA、管理者向けWeb UI、ログイン、エラー・オフライン画面
- 基準言語: 日本語
- 基準タイムゾーン: Asia/Tokyo

本書をUI実装の唯一のデザイン基準とする。画面ごとに別テーマを持ち込まず、最初の縦切り実装からこの方針を適用する。機能要件またはアクセシビリティ要件と衝突する場合は、機能とアクセシビリティを優先する。

## 1. コンセプトと選定理由

「現場の余白 / Field Calm」は、時間に追われやすい打刻操作へ、落ち着きと十分な余白を与えるデザインである。温かいオフホワイトを作業台、深いインク色を読みやすい文字、落ち着いたティールを確実な操作の目印として扱う。アンバーとコーラルは注意や補助に限定し、画面全体を警告色で騒がしくしない。

この方針を採用する理由は次のとおり。

- 従業員には、屋外や移動中でも現在状態と次の一手を迷わず判別できる強い情報階層が必要である。
- 出勤と退勤は日常的で重要な操作なので、派手さよりも位置の一貫性、押しやすさ、処理結果への安心感が重要である。
- 管理者には、多数のカードや装飾ではなく、表の比較可能性と例外状態の発見しやすさが必要である。
- 純白と強い黒の組み合わせを避けることで長時間閲覧時の眩しさを抑えつつ、十分なコントラストを確保できる。
- 単一のティールを操作の軸にすることで、従業員画面と管理者画面に共通のプロダクトらしさを持たせられる。

## 2. 対象ユーザーと体験原則

### 2.1 従業員

- 主端末はスマートフォン、主な利用場面は勤務開始・終了時の短時間操作。
- 一画面一目的とし、「現在どうなっているか」「次に何をするか」を最初に理解できるようにする。
- 主要打刻CTAは状態が変わっても同じ位置を保ち、出勤前は `出勤する`、勤務中は `退勤する`、退勤後は完了表示へ切り替える。
- 打刻前の確認ダイアログは置かない。誤りは実績画面から修正できることを自然に案内する。
- 実績と申請は主要打刻を妨げない下部ナビゲーションから移動できるようにする。

### 2.2 管理者・事務担当

- 主端末はPC、主な利用場面は複数人の状態比較、申請処理、例外確認。
- コンパクトな上部ナビゲーションと表を中心に構成する。恒常的な左サイドバーは設けない。
- フィルターは必要なものだけを表の直前へ置き、選択中の条件と結果の関係を明確にする。
- 状態は短いチップとテキストで示し、詳細は行または明示的なリンクから開く。

## 3. デザイントークン

実装では用途名のセマンティックトークンを参照する。画面内で直接色値や任意の寸法を追加しない。新しい用途が必要な場合は、既存トークンへ割り当てられない理由を確認してから本書へ追加する。

### 3.1 カラー

| トークン | 値 | 用途 |
|---|---:|---|
| `color-canvas` | `#F6F1E7` | アプリ全体の温かいオフホワイト背景 |
| `color-surface` | `#FFFCF5` | カード、パネル、入力領域 |
| `color-surface-raised` | `#FFFFFF` | モーダル、ボトムシート、浮上メニュー |
| `color-surface-muted` | `#ECE9E1` | 無効領域、表ヘッダー、補助背景 |
| `color-ink` | `#172E30` | 見出し、本文、主要数値 |
| `color-ink-muted` | `#526260` | 補足、ラベル、メタ情報 |
| `color-ink-on-strong` | `#FFFFFF` | ティール等の濃色面上の文字 |
| `color-border` | `#D6D9D2` | 標準境界線 |
| `color-border-strong` | `#AEB8B3` | 入力のホバー、表の区切り強調 |
| `color-primary` | `#0B6B63` | 主要CTA、選択中ナビ、主要リンク |
| `color-primary-hover` | `#085A54` | 主要操作のホバー |
| `color-primary-pressed` | `#064B47` | 主要操作の押下 |
| `color-primary-soft` | `#DDEFEA` | 勤務中、選択中、ティールの淡色背景 |
| `color-primary-ink` | `#084C47` | `primary-soft` 上の文字・アイコン |
| `color-focus` | `#147D73` | キーボードフォーカスリング |
| `color-link` | `#09685F` | 本文リンク。本文中では下線も付ける |
| `color-success` | `#1D6B50` | 完了、承認済み |
| `color-success-soft` | `#E1F1E9` | 成功状態の背景 |
| `color-warning` | `#805100` | 申請中、未処理、軽い注意の文字 |
| `color-warning-accent` | `#C27A13` | アンバーのアイコン・左罫線 |
| `color-warning-soft` | `#FFF0C7` | 注意状態の背景 |
| `color-danger` | `#9F3F36` | エラー、却下、破壊的操作 |
| `color-danger-accent` | `#C65A4B` | コーラルのアイコン・左罫線 |
| `color-danger-soft` | `#FBE5E1` | エラー状態の背景 |
| `color-info` | `#315F70` | GPS、補足情報 |
| `color-info-soft` | `#E3F0F3` | 情報表示の背景 |
| `color-overlay` | `rgba(23, 46, 48, 0.48)` | モーダル背面 |
| `color-disabled` | `#899491` | 無効状態の文字・アイコン |

使用規則:

- ティールを唯一の主要操作色とする。出勤と退勤でCTA色を変えず、ラベルと現在状態で意味を伝える。
- アンバーは申請中、未打刻、要確認など、まだ失敗ではない注意に使う。
- コーラルは入力エラー、打刻不備、却下、破壊的操作に限定する。通常の退勤操作には使わない。
- 状態を背景色だけで表さず、必ず日本語ラベルを併記し、必要に応じてアイコンを加える。
- 薄い色面上の本文には対応する濃い `ink` トークンを使用する。アクセント色そのものを小さい本文へ使わない。

### 3.2 タイポグラフィ

| トークン | サイズ / 行高 | 太さ | 用途 |
|---|---:|---:|---|
| `type-xs` | `12px / 18px` | 500 | 補助ラベル、日時の補足。最小サイズ |
| `type-sm` | `14px / 21px` | 400または500 | 表、メタ情報、補助操作 |
| `type-body` | `16px / 24px` | 400 | 標準本文、モバイル入力 |
| `type-label` | `16px / 24px` | 600 | ボタン、フォームラベル、状態 |
| `type-section` | `18px / 27px` | 600 | セクション見出し、カード見出し |
| `type-title` | `24px / 34px` | 700 | 画面タイトル |
| `type-display` | `32px / 40px` | 700 | 主要状態、重要な時刻 |
| `type-clock` | `40px / 44px` | 700 | 当日画面の現在時刻のみ |

- フォントファミリーは `"Noto Sans JP", "Hiragino Sans", "Yu Gothic UI", "Yu Gothic", system-ui, sans-serif` とする。
- 英数字、時刻、所定・実績値には `font-variant-numeric: tabular-nums` を適用し、桁変化による揺れを防ぐ。
- 本文は400、ラベル・表見出しは500〜600、画面タイトルと主要数値は700までとし、800以上は使わない。
- 長い日本語は省略記号だけに頼らず折り返す。氏名や現場名を省略する場合は、詳細で全文を確認できるようにする。

### 3.3 余白

4pxを基準単位とする。

| トークン | 値 | 主用途 |
|---|---:|---|
| `space-0` | `0` | 余白なし |
| `space-1` | `4px` | アイコン内部、密な要素間 |
| `space-2` | `8px` | ラベルと値、チップ間 |
| `space-3` | `12px` | 入力内、表セルの縦余白 |
| `space-4` | `16px` | モバイル標準余白、カード内 |
| `space-5` | `20px` | 関連グループ間 |
| `space-6` | `24px` | セクション内の大きな間隔 |
| `space-8` | `32px` | セクション間 |
| `space-10` | `40px` | 主要状態とCTAの間 |
| `space-12` | `48px` | 画面上部の大きな区切り |
| `space-16` | `64px` | 独立領域間の最大余白 |

- モバイルの左右パディングは16px、430px以上では20px。
- 管理画面の左右パディングは24px、1100px以上では32px。
- 密度を上げる場合も、操作対象間は最低8px、別セクション間は最低24pxを保つ。

### 3.4 サイズ

| トークン | 値 | 用途 |
|---|---:|---|
| `size-touch-min` | `44px` | 全操作対象の最小幅・高さ |
| `size-control` | `44px` | 標準入力、管理画面ボタン |
| `size-cta` | `60px` | 従業員の主要打刻CTA |
| `size-bottom-nav` | `64px` | Safe Areaを除く下部ナビ本体 |
| `size-top-nav` | `64px` | 管理画面の上部ナビ |
| `size-table-row` | `52px` | 管理表の標準行高 |
| `size-icon-sm` | `16px` | 表、入力、チップ |
| `size-icon-md` | `20px` | 標準ボタン、ナビ |
| `size-icon-lg` | `24px` | 従業員の主要ナビ |
| `width-employee` | `560px` | 従業員コンテンツの最大幅 |
| `width-admin` | `1440px` | 管理画面コンテンツの最大幅 |

### 3.5 角丸・境界線・影

| トークン | 値 | 用途 |
|---|---:|---|
| `radius-xs` | `6px` | チップ、小要素 |
| `radius-sm` | `10px` | 入力、管理画面ボタン |
| `radius-md` | `14px` | カード、主要ボタン |
| `radius-lg` | `20px` | ボトムシート、モーダル |
| `radius-pill` | `999px` | 状態チップのみ |
| `border-default` | `1px solid #D6D9D2` | 入力、カード、表 |
| `border-strong` | `1px solid #AEB8B3` | ホバー、区切りの強調 |
| `shadow-low` | `0 1px 2px rgba(23, 46, 48, 0.08)` | 上部ナビ、控えめなカード |
| `shadow-raised` | `0 8px 24px rgba(23, 46, 48, 0.14)` | モーダル、ボトムシート |

- 画面の区切りは影よりも余白、背景差、1px境界線を優先する。
- 通常カードに `shadow-raised` を使わない。浮上要素だけへ限定する。

### 3.6 レイヤー

| トークン | 値 | 用途 |
|---|---:|---|
| `z-base` | `0` | 通常コンテンツ |
| `z-sticky` | `20` | 上部ナビ、下部ナビ、主要CTA領域 |
| `z-popover` | `40` | メニュー、日付選択 |
| `z-modal` | `60` | モーダル、ボトムシート |
| `z-toast` | `80` | トースト、接続状態通知 |

## 4. レスポンシブと基本レイアウト

ブレークポイントは次の2つだけを基本とする。

- `bp-tablet`: `@media (min-width: 768px)`
- `bp-desktop`: `@media (min-width: 1100px)`

任意の端末幅に合わせた場当たり的なブレークポイントは追加しない。

### 4.1 0〜767px

- 従業員画面の標準レイアウト。1カラム、左右16〜20px、最大幅いっぱい。
- 当日画面は `100dvh` を基準にし、主要CTAを画面下半分かつ下部ナビの直上に置く。
- 月次実績は横スクロール表ではなく、日ごとのカードまたは重要項目を絞った行で表示する。
- 管理画面を開いた場合は表の重要列を絞り、残りを行詳細へ移す。デスクトップ表を縮小表示しない。

### 4.2 768〜1099px

- 従業員画面は中央寄せで最大560px。余白を広げても主要CTAの位置関係は変えない。
- 管理画面は上部ナビを維持し、フィルターを必要に応じて2行へ折り返す。
- 管理表は名前、主要時刻、状態を優先し、補助列は詳細行へ移す。列幅を読めないほど縮めない。

### 4.3 1100px以上

- 管理画面の標準レイアウト。最大1440pxで中央寄せ、左右32px。
- フィルターと補助操作を1行にまとめ、表は仕様上の列を比較可能な状態で表示する。
- 従業員画面は最大560pxのまま拡張せず、背景の余白として扱う。

## 5. 従業員向け画面

### 5.1 アプリシェルとナビゲーション

- 上部はサービス名、ログイン中の氏名、アカウントメニューだけの簡潔なヘッダーとする。
- 下部ナビは `今日`、`実績`、`申請` の3項目を固定順で表示する。ログアウトはアカウントメニューへ置く。
- 選択中項目はティールのアイコン、ラベル、上辺または背景差で示す。色だけに依存しない。
- 下部ナビと主要CTAは重ねない。スクロール末尾にも同等の余白を確保する。

### 5.2 当日画面

情報階層を上から次の順に固定する。

1. 日付・曜日と現在時刻
2. 現在の勤務状態
3. 本日の現場、予定時刻、予定休憩
4. 出勤・退勤・休憩の実績
5. 同一位置の主要打刻CTA
6. GPS記録有無、打刻見直し等の補助導線

- 現在時刻は `type-clock`、サーバーに保存された実績は `type-section` までとし、両者を視覚的に区別する。
- 主要CTAは高さ60px、幅100%、`radius-md`。アイコンだけにせず動詞を含むラベルを表示する。
- 処理中は同じ幅・高さを保ち、スピナーと `記録しています` を表示して再押下を無効化する。
- 退勤済みでは同じ領域を成功色の完了パネルへ切り替え、押せるように見せない。
- 勤務予定なし、承認済み非勤務日は主要CTAを出さず、その理由を状態パネルに明記する。
- 申請中バナーはアンバー系を使うが、現在の打刻状態とCTAを隠さない。
- GPS取得中は小さなインライン表示とし、主要操作を位置情報待ちの見た目で停止させない。

### 5.3 月次実績

- 月切り替えは前月、月選択、次月を1つの操作群として画面上部に置く。
- モバイルは日付、区分、出退勤、状態を1カードにまとめる。備考と履歴は詳細で表示する。
- 行・カード全体を開ける場合も、フォーカス可能なリンクまたはボタンとして実装する。
- 未入力、勤務中、完了、申請中、非勤務区分をラベルとアイコンで区別する。

### 5.4 申請

- 作成フォームと申請一覧を同時に詰め込まず、主目的を明示して画面または段階を分ける。
- 区分選択は日本語ラベル付きのラジオグループまたはセレクトを使う。
- 取消はコーラルの塗りつぶしを避け、二次的な破壊操作として表示する。実行時は対象日と区分が分かる確認を行ってよい。

## 6. 管理者向け画面

### 6.1 上部ナビゲーション

- 高さ64pxの上部ナビにプロダクト名、主要ナビ、管理者メニューを置く。
- 主要ナビは `当日`、`現場別`、`申請`、`個人実績`、`監査ログ` の5項目とし、現在地を常に示す。
- 申請中件数は小さな数値バッジで示してよい。バッジだけをリンクにしない。
- 巨大なサイドバー、アイコンだけの折り畳みナビは使用しない。

### 6.2 ページ構造

- ページタイトルと短い説明、コンパクトなフィルターバー、結果要約、表の順に置く。
- KPIカードは設けず、必要な件数は表の上に一文または小さなバッジで示す。
- 日付、現場、従業員、状態フィルターは高さ44pxで揃え、ラベルを常に表示する。
- 主操作は右端へまとめるが、承認・却下を行内へ過密に並べない。狭い場合は詳細へ移す。

### 6.3 表

- 表ヘッダーは `color-surface-muted`、本文は `color-surface`。縦罫線を使わず、行の下境界と余白で読む。
- ヘッダーは左揃えを基本とし、時刻・分数は桁位置が比較できる揃え方に統一する。
- 行高は52px以上。ホバーだけに依存せず、行内に詳細への明示的なリンクを持たせる。
- ステータス列はラベル付きチップを使用する。位置情報は一覧では `あり` / `なし` に留め、座標は詳細だけに表示する。
- 出勤予定超過、打刻不備、申請競合は行背景を全面赤にせず、状態チップと左端の細いアクセントで示す。
- 空のセルは `—` とし、`null`、内部enum、技術的なプレースホルダーを露出しない。
- 1100px未満では重要列を優先し、補助情報は行詳細へ移す。文字を極端に小さくしたり、列を重ねたりしない。

### 6.4 勤務予定の編集

- `当日` の各従業員行から、その日付の勤務予定を登録・更新・削除する。現場別一覧は比較用途を保ち、編集導線を置かない。
- 新規入力は開始09:00、終了18:00、予定休憩60分を初期値とし、勤務場所は管理者が明示的に選ぶまで未選択にする。
- PCでは幅720px以下のモーダル、モバイルでは画面下部のボトムシートとして、現場、開始・終了、予定休憩、任意備考を一続きで編集する。
- 打刻または入力済み実績、申請中・承認済み申請がある日は編集操作を無効化し、その理由を行内へ文字で表示する。
- 削除は従業員名と対象日を含む確認を挟む。登録・更新・削除に理由入力は要求せず、変更者、日時、変更前後を監査ログで確認できるようにする。
- 競合時はダイアログを閉じ、日次一覧を再読込して最新値と理由を表示する。

### 6.5 申請処理と監査

- 承認をティールの主要操作、却下をコーラルの二次操作として区別する。
- 打刻競合で承認できない場合は、理由と解消先の個人実績リンクを同じエラーパネルに表示する。
- 監査ログの前後値は差分が分かる2列または項目単位の表示とし、生のJSONを通常表示しない。
- 変更者、変更日時、理由を差分と同じ文脈で読めるようにする。

## 7. コンポーネント規則

### 7.1 ボタン

- `primary`: ティール塗り＋白文字。1つの操作領域につき原則1個。
- `secondary`: `surface` 背景＋ティール文字＋標準ボーダー。
- `ghost`: 背景なし。ナビゲーションや低優先度操作。
- `danger`: コーラル系。取消、却下、リセット等だけに使用し、通常操作へ使わない。
- 従業員CTAは60px、その他は44px以上。ラベルは `保存` だけでなく、可能なら `修正を保存` のように対象を含める。
- ローディング中も寸法を変えず、二重送信を防止する。無効状態は色・カーソル・属性のすべてで表す。

### 7.2 入力

- ラベルを入力欄の上に常時表示し、プレースホルダーをラベル代わりにしない。
- 高さ44px以上、`radius-sm`、標準ボーダー。フォーカス時は2pxのフォーカスリングを外側へ表示する。
- エラーは対象直下に具体的な日本語で表示し、概要エラーがある場合は対象へ移動できるようにする。
- 時刻入力は `HH:mm`、日付表示は日本語形式。端末ロケールだけに依存しない。
- 任意項目には `任意`、必須項目には必要に応じて `必須` を文字で示す。

### 7.3 カード・パネル

- カードは関連情報をまとめるために使い、すべての値を個別カードにしない。
- 標準は `surface`、`border-default`、`radius-md`、影なしまたは `shadow-low`。
- カード内は16〜24px、カード間は12〜16pxを基準とする。

### 7.4 ステータスチップ

- 高さ24〜28px、左右8px、`radius-pill`、`type-xs` または `type-sm` の600。
- 勤務中はprimary、完了・承認済みはsuccess、申請中・未処理はwarning、打刻不備・却下はdanger、未出勤・取消済みはneutral相当の色を使う。
- `有休`、`欠勤`、`病欠`、`その他` は文字を必須とし、色だけで区分しない。

### 7.5 バナー、エラー、トースト

- 画面継続に影響する通信断・保存不明はインラインバナーで残し、短時間で消えるトーストだけにしない。
- 成功トーストは短い文で3〜5秒を目安に表示し、重要な結果は画面本体にも反映する。
- エラー文は失敗理由と次の操作を示す。例: `保存できませんでした。通信を確認して再試行してください。`
- オフライン時は固定バナーで知らせ、打刻CTAを `通信接続後に再試行` の状態へ切り替える。成功表示は出さない。

### 7.6 モーダル・ボトムシート

- モバイルの短い編集や詳細にはボトムシート、複雑なフォームには独立画面を使う。
- PCでは短い確認を幅560px以下、複数項目を持つ管理者編集を幅720px以下のモーダルとする。長い表や監査履歴はページ内詳細を使う。
- 開いた要素へフォーカスを移し、フォーカストラップ、Escapeで閉じる操作、閉じた後のフォーカス復帰を実装する。
- Safe Areaを含む下余白を確保し、操作ボタンがホームインジケーターへ重ならないようにする。

### 7.7 空・読込状態

- 空状態は `データがありません` だけで終えず、対象月・フィルター等の文脈と利用可能な次の操作を示す。
- スケルトンは実レイアウトと近い寸法にし、読み込み完了時の大きなレイアウト移動を防ぐ。
- スピナー単独表示にはせず、何を読み込んでいるかを短い文で伝える。

## 8. モーション

| トークン | 値 | 用途 |
|---|---:|---|
| `motion-fast` | `120ms` | ホバー、押下、フォーカス |
| `motion-base` | `180ms` | チップ、状態、インライン要素 |
| `motion-slow` | `240ms` | モーダル、ボトムシート |
| `ease-standard` | `cubic-bezier(0.2, 0, 0, 1)` | 標準遷移 |
| `ease-exit` | `cubic-bezier(0.4, 0, 1, 1)` | 閉じる遷移 |

- 変化させるのは原則 `opacity` と `transform`。`transition: all` は使用しない。
- 打刻成功はチェック表示とごく小さいフェード／スケールで伝え、紙吹雪、バウンド、長い演出は使わない。
- 現在時刻の更新でアニメーションさせない。
- `prefers-reduced-motion: reduce` では移動・拡大をなくし、必要な状態変化は即時または短いフェードだけにする。

## 9. Safe AreaとPWA

- viewportに `viewport-fit=cover` を設定する。
- 画面上端は `max(16px, env(safe-area-inset-top))`、左右は基準余白に `env(safe-area-inset-left/right)` を加味する。
- 下部ナビは本体64pxに `env(safe-area-inset-bottom)` を加え、内部操作はSafe Areaより上へ置く。
- 固定CTAを使う場合、下端は `64px + env(safe-area-inset-bottom) + 12px` 以上を確保する。
- スクロール領域末尾には、CTA・下部ナビと内容が重ならないパディングを持たせる。
- Manifestの `theme-color` は `color-primary`、背景色は `color-canvas` を基本とする。
- スタンドアロン表示でもブラウザ表示でも、戻る・ログアウト・エラー復帰の導線を失わない。

## 10. アクセシビリティ

WCAG 2.2 AA相当を基準とする。

- 通常文字は背景とのコントラスト比4.5:1以上、大きい文字とUI境界は3:1以上を確保する。
- 操作対象は原則44×44px以上。近接する操作間には誤タップを避ける余白を持たせる。
- 全操作をキーボードで実行可能にし、論理的なフォーカス順とスキップリンクを提供する。
- フォーカスリングを消さない。背景から判別できる2px以上のリングと2px程度のオフセットを使う。
- 見出し階層、`main`、`nav`、フォーム、表の見出しセル・captionを意味どおりに使用する。
- アイコンだけのボタンは避け、必要な場合はアクセシブルネームとツールチップを付ける。
- 入力エラーは `aria-describedby` 等で対象入力と結び、先頭のエラー概要から各項目へ移動可能にする。
- 非同期の保存成功・失敗は適切なライブリージョンで通知する。毎秒更新する現在時刻をライブリージョンにしない。
- 色、位置、形だけで状態を伝えず、日本語ラベルを必ず併記する。
- 表示言語を日本語に固定し、曜日・日時を読み上げても意味が通る形式にする。
- モーダル・ボトムシートはフォーカス管理を行い、背景へフォーカスが抜けないようにする。
- 200%拡大時にも主要操作、フォーム、エラーが欠落せず、従業員画面に横スクロールを発生させない。

## 11. 文言とアイコン

- 文言は短く具体的な日本語にし、`pending`、`attendanceCategory`、HTTPコード等の内部語を表示しない。
- ボタンは `する`、`保存する`、`取消する` など、操作結果が予測できる動詞を使う。
- 日時は `2026年8月10日（月）`、時刻は `09:05` のように表し、曜日はAsia/Tokyo基準で生成する。
- アイコンは単一の線画セットで統一し、装飾目的で複数スタイルを混在させない。
- 絵文字を状態アイコンや主要ナビとして使わない。
- 位置情報は `位置情報あり`、`位置情報なし` のように価値判断を含まない文言にする。

## 12. 禁止事項

- 複数のデザインテーマ、画面単位の独自色・独自余白を混在させない。
- 巨大な常設サイドバー、多数のKPIカード、カード内カード、過剰な表列を作らない。
- ティール以外を通常の主要CTAに使わない。アンバーやコーラルで画面全体を塗らない。
- グラデーション、ネオン、ガラス調、強いドロップシャドウ、意味のない装飾パターンを使わない。
- 状態を色だけ、アイコンだけ、薄いコントラストだけで表現しない。
- フォーカス表示を消さない。マウスホバーだけで情報や操作を提供しない。
- プレースホルダーをラベル代わりにしない。アイコンだけの未命名ボタンを置かない。
- 従業員画面に横スクロール表を置かない。主要打刻CTAの位置を状態ごとに移動させない。
- 出退勤前に毎回確認ダイアログを挟まない。GPS取得を打刻成功の必須条件に見せない。
- 無効状態を透明度だけで表さない。処理中にボタン寸法やレイアウトを変えない。
- 全要素へアニメーションを付けない。`transition: all`、点滅、長い成功演出を使わない。
- 管理表に生のID、enum、JSON、緯度経度を常時表示しない。
- オフライン時や保存結果不明時に打刻成功を表示しない。
- 技術用語、責任を利用者へ押し付ける文言、原因と復帰方法のないエラーを使わない。

## 13. UI実装の確認基準

- 390×844、430×932で従業員画面に横スクロールがなく、主要CTAと下部ナビがSafe Areaへ干渉しない。
- 768×1024で従業員画面の幅が過剰に広がらず、管理画面のフィルターと表が破綻しない。
- 1440×900で管理画面がコンパクトな上部ナビと比較しやすい表を維持し、空白をKPIカードで埋めていない。
- 出勤前、勤務中、退勤済み、勤務予定なし、申請中、承認済み非勤務、通信エラーを色以外でも識別できる。
- キーボードだけでログイン、打刻、月移動、申請、承認・却下、修正、ログアウトを実行できる。
- 200%拡大、`prefers-reduced-motion`、オフライン、GPS拒否でも状態と次の操作が理解できる。
- Manifest、Service Worker、スタンドアロン表示で背景色・テーマ色・Safe Areaが本書と一致する。
